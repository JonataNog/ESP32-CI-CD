name: ESP32-CI-CD
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repo checkout
        uses: actions/checkout@v3
        with:
          submodules: 'recursive'

      - name: Espressif IoT Development Framework (ESP-IDF)
        uses: espressif/esp-idf-ci-action@v1.1.0
        with:
          esp_idf_version: v4.4
          target: esp32

      - name: Store Artifacts
        uses: actions/upload-artifact@v3
        with: 
          name: ESP32-CI-CD
          path: |
            build/ESP32-CI-CD.bin

  test:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Repo checkout
        uses: actions/checkout@v3

      - name: Build unit tests
        uses: espressif/esp-idf-ci-action@v1.1.0
        with:
          esp_idf_version: v4.4
          target: esp32
          path: test

      - name: Store Artifacts
        uses: actions/upload-artifact@v3
        with: 
          name: unit-test
          path: |
            test/build/unitExample.bin

#  runner:
#    runs-on: ubuntu-latest
#    needs: [test]
#    steps:
#      - name: Repo checkout
#        uses: actions/checkout@v3

#      - name: ESP32 QEmu Runner
#        uses: tobozo/esp32-qemu-sim@1.0.0
#        with:
#         flash-size: 4
#         build-folder: build
#         qemu-timeout: "60"

#      - name: ESP32 Unit Test QEmu Runner
#        uses: tobozo/esp32-qemu-sim@1.0.0
#        with:
#          flash-size: 4
#          build-folder: test/build
#          qemu-timeout: "60"        
  
